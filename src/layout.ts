// Content is capped to roughly a tablet's portrait width and centred, so the
// app doesn't stretch awkwardly wide on tablets / large screens.
export const MAX_CONTENT_WIDTH = 768;

// Tighter cap (≈ a phone in portrait) for the menu buttons and the
// staff / note display, so they stay compact even on wide screens.
export const PHONE_CONTENT_WIDTH = 430;
